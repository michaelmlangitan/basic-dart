void main() {
  // simbol mirip seperti constant.
  // Ketika menggunakan # tidak boleh ada spasi, Jika ingin ada spasi gunakan Symbol construct.

  Symbol symbol1 = Symbol('Michael Langitan');
  var symbol2 = #guide_symbol;


  print(symbol1);
  print(symbol2);
}